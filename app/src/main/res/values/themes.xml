<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MoneyDebtor" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/lightred</item>
        <item name="colorPrimaryDark">@color/lightred</item>
        <item name="colorOnBackground">@color/bg_light</item>
        <item name="colorAccent">@color/lightred</item>
        <item name="colorControlNormal">@color/black</item>
    </style>

    <style name="EditTextColor">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.MoneyDebtor" parent="Base.Theme.MoneyDebtor" />
</resources>